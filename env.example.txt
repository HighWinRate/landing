# Backend API URL
# Development: http://localhost:3000
# Production: https://api.highwinrate.com
NEXT_PUBLIC_API_URL=http://localhost:3000

# Frontend URL (برای لینک‌های ورود/ثبت‌نام/محصولات)
# Development: http://localhost:3001
# Production: https://app.highwinrate.com (یا URL اصلی frontend)
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3001

# Blog Ghost CMS URL (برای Next.js Rewrite)
# این URL به صورت داخلی استفاده می‌شود (Next.js این را به /blog proxy می‌کند)
# Development: http://localhost:3004
# Production: https://blog-internal.highwinrate.com (Ghost server internal URL)
BLOG_URL=http://localhost:3004

# نکته: کاربران از /blog استفاده می‌کنند (highwinrate.com/blog) - این برای SEO بهتر است
# Ghost Admin را می‌توانید از BLOG_URL/ghost دسترسی داشته باشید

# Supabase Configuration (برای چک کردن وضعیت authentication)
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
