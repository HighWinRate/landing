# Backend API URL
# Development: http://localhost:3000
# Production: https://api.highwinrate.com
NEXT_PUBLIC_API_URL=http://localhost:3000

# Frontend URL (برای لینک‌های ورود/ثبت‌نام/محصولات)
# Development: http://localhost:3001
# Production: https://app.highwinrate.com (یا URL اصلی frontend)
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3001

# Sanity Configuration (برای Blog)
# Public Environment Variables
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production

# Alternative Sanity Variables (optional)
SANITY_API_PROJECT_ID=your-project-id
SANITY_API_DATASET=production
SANITY_STUDIO_PROJECT_ID=your-project-id
SANITY_STUDIO_DATASET=production

# Private/Secret Environment Variables (server-side only)
SANITY_API_READ_TOKEN=your-read-token
SANITY_API_WRITE_TOKEN=your-write-token

# Note: Blog is now integrated in landing (monorepo) - no separate URL needed

# Supabase Configuration (برای چک کردن وضعیت authentication)
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
