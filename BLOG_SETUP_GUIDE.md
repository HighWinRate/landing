# راهنمای کامل راه‌اندازی بلاگ با Payload CMS

## 📋 مراحل راه‌اندازی

### مرحله 1: تنظیم متغیرهای محیطی

فایل `.env` را در پوشه `landing` ایجاد کنید:

```bash
cd landing
cp env.example.txt .env
```

سپس فایل `.env` را باز کنید و این متغیرها را تنظیم کنید:

```env
# Payload CMS Configuration
PAYLOAD_SECRET=your-secret-key-here
# Development: http://localhost:3003
# Production: https://highwinrate.com
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3003

# Database Configuration (Supabase)
# روش 1: استفاده از متغیرهای جداگانه (پیشنهادی)
POSTGRES_HOST=db.xxxxxxxxxxxxx.supabase.co
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-database-password
POSTGRES_DATABASE=postgres

# یا روش 2: استفاده از SUPABASE_URL (host خودکار استخراج می‌شود)
SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
# یا
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
```

**نحوه تولید PAYLOAD_SECRET:**

```bash
openssl rand -base64 32
```

**نحوه دریافت اطلاعات Supabase:**

1. به داشبورد Supabase بروید: https://supabase.com/dashboard
2. پروژه خود را انتخاب کنید
3. به **Settings** > **Database** بروید
4. در بخش "Connection string" اطلاعات زیر را پیدا کنید:
   - **Host**: `db.xxxxxxxxxxxxx.supabase.co`
   - **Port**: `5432`
   - **Database**: معمولاً `postgres`
   - **User**: معمولاً `postgres`
   - **Password**: رمز عبور database شما

### مرحله 2: اجرای Migration

برای ایجاد جداول در پایگاه داده Supabase:

**روش 1: استفاده از script (پیشنهادی)**

```bash
cd landing
npm run payload:migrate
```

**روش 2: استفاده از Payload CLI**

```bash
npx payload migrate
```

**روش 3: در Vercel (بعد از deploy)**

```bash
# Pull environment variables
vercel env pull .env.local

# Run migration
npm run payload:migrate
```

این دستور جداول زیر را در Supabase ایجاد می‌کند:

- `users` - کاربران
- `posts` - پست‌های بلاگ
- `authors` - نویسندگان
- `categories` - دسته‌بندی‌ها
- `media` - فایل‌های رسانه‌ای
- `payload_migrations` - تاریخچه migration ها
- `payload_preferences` - تنظیمات Payload

**⚠️ مهم:** اگر migration کار نکرد، به `MIGRATION_GUIDE.md` مراجعه کنید.

### مرحله 3: راه‌اندازی سرور

```bash
cd landing
npm run dev
```

سرور در آدرس `http://localhost:3003` اجرا می‌شود.

### مرحله 4: دسترسی به Admin Panel

1. به آدرس `http://localhost:3003/admin` بروید
2. اولین بار که وارد می‌شوید، باید یک کاربر admin ایجاد کنید:
   - **Email**: ایمیل خود را وارد کنید
   - **Password**: یک رمز عبور قوی انتخاب کنید
   - **Name**: نام خود را وارد کنید (اختیاری)

### مرحله 5: ایجاد محتوا

بعد از ورود به Admin Panel:

#### 5.1. ایجاد Author (نویسنده)

1. در منوی سمت چپ روی **"Authors"** کلیک کنید
2. روی دکمه **"Create New"** کلیک کنید
3. فیلدها را پر کنید:
   - **Name**: نام نویسنده (مثلاً: "علی احمدی")
   - **Slug**: به صورت خودکار از نام ساخته می‌شود (مثلاً: "ali-ahmadi")
   - **Image**: تصویر نویسنده (اختیاری)
   - **Bio**: بیوگرافی نویسنده (Rich Text)
   - **Social Links**: لینک‌های شبکه‌های اجتماعی (اختیاری)
     - Twitter
     - LinkedIn
     - GitHub
4. روی **"Save"** کلیک کنید

#### 5.2. ایجاد Category (دسته‌بندی)

1. در منوی سمت چپ روی **"Categories"** کلیک کنید
2. روی دکمه **"Create New"** کلیک کنید
3. فیلدها را پر کنید:
   - **Title**: عنوان دسته‌بندی (مثلاً: "معاملات")
   - **Slug**: به صورت خودکار از عنوان ساخته می‌شود (مثلاً: "trading")
   - **Description**: توضیحات دسته‌بندی (اختیاری)
4. روی **"Save"** کلیک کنید

#### 5.3. ایجاد Post (پست)

1. در منوی سمت چپ روی **"Posts"** کلیک کنید
2. روی دکمه **"Create New"** کلیک کنید
3. فیلدها را پر کنید:

   **اطلاعات اصلی:**
   - **Title**: عنوان پست (مثلاً: "راهنمای کامل معاملات")
   - **Slug**: به صورت خودکار از عنوان ساخته می‌شود
   - **Author**: از لیست نویسنده را انتخاب کنید
   - **Published At**: تاریخ انتشار را انتخاب کنید
   - **Status**: "Published" را انتخاب کنید (برای انتشار)

   **محتوا:**
   - **Main Image**: تصویر اصلی پست (اختیاری)
   - **Excerpt**: خلاصه پست (حداکثر 200 کاراکتر)
   - **Body**: محتوای اصلی پست (Rich Text Editor)
     - می‌توانید از formatting tools استفاده کنید
     - می‌توانید تصاویر اضافه کنید
     - می‌توانید لینک اضافه کنید

   **دسته‌بندی:**
   - **Categories**: یک یا چند دسته‌بندی انتخاب کنید

   **SEO (اختیاری):**
   - **Meta Title**: عنوان SEO
   - **Meta Description**: توضیحات SEO
   - **Keywords**: کلمات کلیدی

4. روی **"Save"** کلیک کنید

### مرحله 6: مشاهده بلاگ

بعد از ایجاد پست‌ها:

1. به آدرس `http://localhost:3003/blog` بروید
2. لیست پست‌ها را می‌بینید
3. روی هر پست کلیک کنید تا محتوای کامل را ببینید

## 🎯 ساختار URL ها

- **صفحه اصلی بلاگ**: `/blog`
- **صفحه پست**: `/blog/[slug]`
- **صفحه نویسنده**: `/blog/author/[slug]`
- **صفحه دسته‌بندی**: `/blog/category/[slug]`
- **Admin Panel**: `/admin`

## 🔧 دستورات مفید

```bash
# Development
npm run dev

# Build
npm run build

# Start Production
npm run start

# Payload CLI
npm run payload

# Migration
npm run payload migrate
# یا
npx payload migrate
```

## ⚠️ مشکلات رایج و راه حل

### مشکل: "PAYLOAD_SECRET is required"

**راه حل:**

- بررسی کنید که `PAYLOAD_SECRET` در فایل `.env` تنظیم شده است
- سرور را restart کنید

### مشکل: "Database connection failed"

**راه حل:**

- بررسی کنید که `POSTGRES_PASSWORD` تنظیم شده است
- بررسی کنید که `POSTGRES_HOST` یا `SUPABASE_URL` صحیح است
- بررسی کنید که Supabase project شما active است

### مشکل: "Cannot find module 'payload'"

**راه حل:**

```bash
npm install
```

### مشکل: Admin Panel باز نمی‌شود

**راه حل:**

- بررسی کنید که سرور در حال اجرا است
- بررسی کنید که `PAYLOAD_PUBLIC_SERVER_URL` صحیح است
- بررسی console برای خطاها

### مشکل: Migration اجرا نمی‌شود

**راه حل:**

- بررسی کنید که متغیرهای database تنظیم شده‌اند
- بررسی کنید که به Supabase دسترسی دارید
- بررسی کنید که جداول قبلاً ایجاد نشده باشند

### مشکل: "self-signed certificate in certificate chain"

**علت:** Supabase از SSL استفاده می‌کند و Node.js نمی‌تواند certificate را verify کند.

**راه حل:** این مشکل به صورت خودکار در کد حل شده است. اگر هنوز خطا می‌دهد:

- بررسی کنید که آخرین version کد را دارید
- سرور را restart کنید
- بررسی کنید که connection string یا متغیرهای database صحیح هستند

## 📝 نکات مهم

1. **اول Author و Category ایجاد کنید**، سپس Post
2. **Status را "Published" قرار دهید** تا پست در بلاگ نمایش داده شود
3. **Slug ها باید unique باشند** - Payload به صورت خودکار از Title/Name می‌سازد
4. **Rich Text Editor** از Lexical استفاده می‌کند - امکانات زیادی دارد
5. **تصاویر** در collection "Media" ذخیره می‌شوند

## 🚀 بعد از راه‌اندازی

بعد از اینکه بلاگ را راه‌اندازی کردید:

1. **ایجاد محتوا**: چند پست نمونه ایجاد کنید
2. **تست کردن**: صفحات مختلف را تست کنید
3. **SEO**: Meta tags را برای هر پست تنظیم کنید
4. **Deploy**: برای production، به `VERCEL_DEPLOY.md` مراجعه کنید

## 📚 مستندات بیشتر

- `PAYLOAD_SETUP.md` - راهنمای کامل Payload
- `VERCEL_DEPLOY.md` - راهنمای Deploy در Vercel
- `README.md` - مستندات کلی پروژه

---

**موفق باشید! 🎉**
